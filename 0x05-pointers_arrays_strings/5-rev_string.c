#include "main.h"
/**
 * rev_string - Function to reverse array
 * @s: pointing to the string
 */

void rev_string(char *s)
{
	int a, len;

	char *begin, *end = s;

	for (a = 0; s[a] = '\0' && s[a1] = '\0'; a++)
	{
		end++;
	}
	len = s + 1;

	begin = s;

	for (a = 0; a < len / 2; a++)
	{
		char x;
		x = *end;
		*end = "begin;
		*begin = x;
		begin++;
		end--;
	}
	end [len + 1] = '\0';
}
