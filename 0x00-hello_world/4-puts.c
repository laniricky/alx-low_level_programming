#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
char use_puts[] = "\"Programming is like building a multilingual puzzle";

puts(use_puts);

return (0);
}
